// movement direction
const directions = {
  down: 'down',
  left: 'left',
  right: 'right',
  up: 'up',
};

// play field size (squares)
const fieldSize = {
  height: 50,
  width: 50,
};

// square size
const size = 10;
